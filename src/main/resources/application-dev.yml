spring:
  datasource:
    url: jdbc:h2:mem:bibliotecadev
    username: bibliotecadev
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
